## Packages
framer-motion | Smooth animations for panels and transitions
clsx | Conditional class names
tailwind-merge | Merging tailwind classes
react-hook-form | Form handling
@hookform/resolvers | Zod resolvers for forms

## Notes
Theme is persisted in localStorage key 'theme' (light/dark)
AI Tutor uses SSE streaming from POST endpoint
Crypto operations are performed server-side for security simulation
